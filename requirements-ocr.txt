# Optional: enables the "PaddleOCR + Text LLM" extraction mode in
# enhanced_ui.py (ocr_hybrid.py). Not part of the default install because
# it's heavy - paddlepaddle alone is ~100MB (macOS ARM) to ~186MB (Linux),
# plus ~250-400MB of recognition/detection models PaddleOCR downloads on
# first use per language. Install into a virtual environment, not your
# system/global Python - see README's OCR Architecture section.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt -r requirements-ocr.txt

paddlepaddle
paddleocr
